import 'babel-polyfill';
import React from 'react';
import ReactDOM from 'react-dom';

const About = (props) => {
    return(
      <div>
        <h1>About Page</h1>
      </div>
    )
  }

export default About;
