import 'babel-polyfill';
import React from 'react';
import ReactDOM from 'react-dom';

const Archery = (props) => {
    return(
      <div>
        <h1>Archery Page</h1>
      </div>
    )
  }

export default Archery;
