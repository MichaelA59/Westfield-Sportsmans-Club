import 'babel-polyfill';
import React from 'react';
import ReactDOM from 'react-dom';

const Events = (props) => {
    return(
      <div>
        <h1>Events Page</h1>
      </div>
    )
  }

export default Events;
